services:
  - type: web
    name: visual-data-science-dashboard
    env: python
    plan: free
    buildCommand: python -m pip install -U pip setuptools wheel && pip install -r requirements.txt
    startCommand: gunicorn app:server
